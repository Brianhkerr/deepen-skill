# Examples

This folder holds a **real `/deepen` run** as the hero example — a complete knowledge base
on a neutral public topic, so prospective users can see actual output (stances, falsifiers,
track-record-weighted experts, ranked tactics) rather than a mockup.

> Per the skill's own honesty rule, the example must be a genuine run with real citations —
> never a fabricated sample. It will be generated by running `/deepen` on a public topic and
> committing the resulting `knowledge/<topic>/` folder here.

**Status:** placeholder — hero example to be generated and dropped in before publishing.
